from setuptools import setup

setup(
    name="nawi-bot",
    version="1.0",
    install_requires=[
        "pyTelegramBotAPI==4.14.0",
        "requests==2.31.0", 
        "python-dotenv==1.0.0"
    ]
)